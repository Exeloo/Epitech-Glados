function size_of_array(arr) {
    array_size = 0;
    a = arr[array_size];
    while (a != undefined) {
        array_size = array_size + 1;
        a = arr[array_size];
    }
    return array_size;
}

function wolfram(size, line) {
    last_line = [true];
    for (j = 0; j < line; j = j + 1) {

        arr = last_line;
        for (i = 0; i < size; i = i + 1) {
        }
    }
}

print(size_of_array([]));
print(size_of_array([1,2,3,4,5,6]));
wolfram(5, 8);
